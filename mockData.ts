// This file is no longer needed as all data is now being fetched from Firebase.
// Keeping the file to prevent import errors in case it's referenced elsewhere,
// but all mock data has been removed.
