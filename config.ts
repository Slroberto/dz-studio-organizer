// This file previously contained configuration constants for Google APIs (Client ID, API Key, Sheets ID).
// Since the application has been migrated to use Firebase, these are no longer needed.
// The Firebase configuration is now centralized in `firebase.ts`.
